"""Put the repo root on sys.path so tests can import the worker shims."""

# Presence of this file puts the repo root on sys.path so tests can
# `import scikit_bio_worker`, `import serve`, and `import vgi_scikit_bio`.
